{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {<% if (compassBootstrap) { %>
    "sass-bootstrap": "latest",<% } %><% if (includeRequireJS) { %>
    "requirejs": "latest",<% } %>
    "modernizr": "latest",
    "jquery": "latest",
    "sp2013-css": "latest"
  },
  "devDependencies": {}
}
